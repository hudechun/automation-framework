# AI论文写作系统 - 前端路由配置指南

## 📋 路由系统说明

RuoYi-Vue3-FastAPI使用**动态路由系统**，路由配置从后端菜单数据动态生成，而不是在前端静态配置。

### 工作原理

1. **后端菜单数据** → 数据库`sys_menu`表存储菜单结构
2. **用户登录** → 后端返回用户可访问的菜单列表
3. **动态生成路由** → 前端根据菜单数据动态生成路由配置
4. **组件加载** → 根据菜单的`component`字段动态加载对应组件

---

## ✅ 已完成工作

### 1. 数据库菜单配置 ✅
已通过SQL脚本配置完成（`thesis_menus.sql`）：
- 1个一级菜单：AI论文写作
- 5个二级菜单：会员管理、论文管理、模板管理、订单管理、支付管理
- 8个三级菜单：具体功能页面
- 33个按钮权限

### 2. 前端组件文件 ✅
已创建所有8个页面组件：
```
src/views/thesis/
├── member/
│   ├── package.vue     ✅ 会员套餐管理
│   ├── user.vue        ✅ 用户会员管理
│   └── quota.vue       ✅ 配额管理
├── paper/
│   └── list.vue        ✅ 论文列表管理
├── template/
│   └── list.vue        ✅ 模板管理
├── order/
│   └── list.vue        ✅ 订单管理
└── payment/
    ├── config.vue      ✅ 支付配置
    └── transaction.vue ✅ 交易记录
```

### 3. 路由配置文件 ✅
创建了`src/router/thesis.js`作为参考配置

---

## 🔧 菜单与组件映射关系

### 数据库菜单配置
在`sys_menu`表中，每个菜单项的`component`字段指定了组件路径：

| 菜单名称 | component字段 | 对应组件文件 |
|---------|--------------|-------------|
| 会员套餐 | `thesis/member/package` | `src/views/thesis/member/package.vue` |
| 用户会员 | `thesis/member/user` | `src/views/thesis/member/user.vue` |
| 配额管理 | `thesis/member/quota` | `src/views/thesis/member/quota.vue` |
| 论文列表 | `thesis/paper/list` | `src/views/thesis/paper/list.vue` |
| 模板管理 | `thesis/template/list` | `src/views/thesis/template/list.vue` |
| 订单管理 | `thesis/order/list` | `src/views/thesis/order/list.vue` |
| 支付配置 | `thesis/payment/config` | `src/views/thesis/payment/config.vue` |
| 交易记录 | `thesis/payment/transaction` | `src/views/thesis/payment/transaction.vue` |

### 组件命名规范
- **路径格式**: `模块/子模块/页面`
- **文件位置**: `src/views/模块/子模块/页面.vue`
- **组件名称**: 使用PascalCase，如`MemberPackage`

---

## 🚀 使用步骤

### 1. 确认数据库菜单已导入
```sql
-- 检查菜单是否已导入
SELECT menu_id, menu_name, component, perms 
FROM sys_menu 
WHERE menu_name LIKE '%论文%' OR menu_name LIKE '%会员%';
```

应该看到39条记录（1个一级 + 5个二级 + 8个三级 + 25个按钮）。

### 2. 确认用户有访问权限
```sql
-- 为admin用户分配论文系统权限
-- 方式1: 通过角色分配（推荐）
-- 在系统管理 → 角色管理 → 编辑角色 → 菜单权限中勾选"AI论文写作"

-- 方式2: 直接SQL（开发环境）
INSERT INTO sys_role_menu (role_id, menu_id)
SELECT 1, menu_id FROM sys_menu WHERE menu_name LIKE '%论文%' OR menu_name LIKE '%会员%';
```

### 3. 登录系统查看菜单
1. 启动前端服务：`npm run dev`
2. 访问：`http://localhost:80`
3. 登录账号：`admin / admin123`
4. 左侧菜单应显示"AI论文写作"模块

### 4. 测试页面访问
点击各个菜单项，确认页面能正常加载和显示。

---

## 🎯 权限控制

### 菜单权限
- 通过`sys_menu`表的`perms`字段控制
- 格式：`模块:功能:操作`
- 示例：`thesis:member:list`、`thesis:order:add`

### 按钮权限
在组件中使用`v-hasPermi`指令：
```vue
<el-button
  type="primary"
  icon="Plus"
  @click="handleAdd"
  v-hasPermi="['thesis:member:add']"
>新增</el-button>
```

### API权限
后端通过装饰器控制：
```python
@router.get("/list")
@require_permission("thesis:member:list")
async def list_packages():
    pass
```

---

## 📊 菜单结构

```
AI论文写作 (thesis)
├── 会员管理 (member)
│   ├── 会员套餐 (package)
│   │   ├── 查询 [thesis:package:query]
│   │   ├── 新增 [thesis:package:add]
│   │   ├── 修改 [thesis:package:edit]
│   │   └── 删除 [thesis:package:remove]
│   ├── 用户会员 (user)
│   │   ├── 查询 [thesis:member:query]
│   │   ├── 开通 [thesis:member:add]
│   │   ├── 修改 [thesis:member:edit]
│   │   ├── 删除 [thesis:member:remove]
│   │   └── 续费 [thesis:member:renew]
│   └── 配额管理 (quota)
│       ├── 查询 [thesis:quota:query]
│       ├── 充值 [thesis:quota:recharge]
│       └── 导出 [thesis:quota:export]
├── 论文管理 (paper)
│   └── 论文列表 (list)
│       ├── 查询 [thesis:paper:query]
│       ├── 新增 [thesis:paper:add]
│       ├── 修改 [thesis:paper:edit]
│       ├── 删除 [thesis:paper:remove]
│       ├── 生成 [thesis:paper:generate]
│       └── 导出 [thesis:paper:export]
├── 模板管理 (template)
│   └── 模板列表 (list)
│       ├── 查询 [thesis:template:query]
│       ├── 上传 [thesis:template:upload]
│       ├── 修改 [thesis:template:edit]
│       ├── 删除 [thesis:template:remove]
│       └── 应用 [thesis:template:apply]
├── 订单管理 (order)
│   └── 订单列表 (list)
│       ├── 查询 [thesis:order:query]
│       ├── 取消 [thesis:order:cancel]
│       └── 退款 [thesis:order:refund]
└── 支付管理 (payment)
    ├── 支付配置 (config)
    │   ├── 查询 [thesis:payment:query]
    │   ├── 配置 [thesis:payment:config]
    │   └── 测试 [thesis:payment:test]
    └── 交易记录 (transaction)
        ├── 查询 [thesis:transaction:query]
        ├── 同步 [thesis:transaction:sync]
        └── 导出 [thesis:transaction:export]
```

---

## 🔍 故障排查

### 问题1: 菜单不显示
**原因**: 
- 数据库菜单未导入
- 用户角色没有分配菜单权限

**解决**:
```sql
-- 检查菜单
SELECT * FROM sys_menu WHERE menu_name = 'AI论文写作';

-- 检查角色权限
SELECT rm.*, m.menu_name 
FROM sys_role_menu rm
JOIN sys_menu m ON rm.menu_id = m.menu_id
WHERE rm.role_id = 1 AND m.menu_name LIKE '%论文%';
```

### 问题2: 点击菜单报404
**原因**: 
- 组件路径不匹配
- 组件文件不存在

**解决**:
1. 检查`sys_menu`表的`component`字段
2. 确认`src/views/`下对应文件存在
3. 检查组件文件的`name`属性

### 问题3: 按钮权限不生效
**原因**: 
- `v-hasPermi`指令的权限标识错误
- 后端未返回对应权限

**解决**:
1. 检查按钮的`v-hasPermi`值
2. 查看浏览器控制台的用户权限列表
3. 确认数据库`sys_menu`的`perms`字段

---

## 📝 开发建议

### 1. 新增页面流程
1. 创建Vue组件文件
2. 在数据库添加菜单记录
3. 分配角色权限
4. 测试访问

### 2. 组件开发规范
- 使用`<script setup name="ComponentName">`
- 组件名称与文件名对应
- 使用RuoYi提供的工具函数和组件

### 3. 权限控制规范
- 菜单权限：控制菜单显示
- 按钮权限：控制操作按钮
- API权限：后端接口验证

---

## ✅ 完成状态

- ✅ 数据库菜单配置完成
- ✅ 前端组件文件完成
- ✅ 路由配置文件完成
- ✅ 权限标识配置完成
- ⏳ 等待测试验证

---

**文档版本**: 1.0  
**创建时间**: 2026-01-25  
**适用系统**: RuoYi-Vue3-FastAPI

