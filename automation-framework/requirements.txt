# ============================================
# 核心依赖
# ============================================
playwright==1.40.0
fastapi==0.104.1
uvicorn[standard]==0.24.0
# 已迁移到 SQLAlchemy，不再使用 Tortoise ORM
# tortoise-orm==0.20.0  # 已移除
# aiomysql==0.2.0  # 已移除（SQLAlchemy 使用 asyncmy）
# aerich==0.7.2  # 已移除（使用 Alembic 进行迁移）

# SQLAlchemy 相关（统一使用 RuoYi 的数据库连接）
sqlalchemy>=2.0.0
asyncmy>=0.2.9  # MySQL 异步驱动（SQLAlchemy 使用）
apscheduler==3.10.4

# FastAPI相关
starlette==0.27.0  # FastAPI依赖，WebSocket支持
websockets==12.0  # WebSocket支持

# 桌面驱动依赖
pywinauto==0.6.8; sys_platform == 'win32'
pyobjc-framework-Cocoa==10.0; sys_platform == 'darwin'
pyobjc-framework-Quartz==10.0; sys_platform == 'darwin'
python-xlib==0.33; sys_platform == 'linux'
pyatspi==2.46.0; sys_platform == 'linux'

# AI依赖
openai==1.3.0
anthropic==0.7.0

# 安全和加密
keyring==24.3.0
cryptography==41.0.7

# ============================================
# 工具库
# ============================================
pydantic==2.5.0
python-dotenv==1.0.0
aiofiles==23.2.1
httpx==0.25.2
aiohttp==3.9.1  # Ollama provider使用

# ============================================
# 测试依赖
# ============================================
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# ============================================
# 日志和监控
# ============================================
python-json-logger==2.0.7
psutil==5.9.6

# ============================================
# CLI工具
# ============================================
click==8.1.7
rich==13.7.0
typer==0.9.0  # CLI框架（可选）

# ============================================
# 其他工具
# ============================================
pyyaml==6.0.1

# ============================================
# 认证和安全
# ============================================
pyjwt==2.8.0
passlib[bcrypt]==1.7.4  # 密码哈希
python-jose[cryptography]==3.3.0  # JWT处理（可选）

# ============================================
# 模板和渲染
# ============================================
jinja2==3.1.2

# ============================================
# 管理后台
# ============================================
# 使用自定义HTML模板，不需要额外依赖
# jinja2已经包含在FastAPI中

# ============================================
# 文件处理
# ============================================
python-multipart==0.0.6  # 文件上传支持
# pillow==10.1.0  # 图像处理（截图处理）- 可选，如需要请单独安装

# ============================================
# 通知系统
# ============================================
aiosmtplib==3.0.1  # 异步邮件发送

# ============================================
# 报告生成（可选）
# ============================================
reportlab==4.0.7  # PDF报告生成
openpyxl==3.1.2  # Excel报告生成（可选）

# ============================================
# 数据库驱动（可选）
# ============================================
# asyncpg==0.29.0  # PostgreSQL支持（可选）
# aiosqlite==0.19.0  # SQLite支持（可选）

# ============================================
# 缓存（可选）
# ============================================
redis==5.0.1  # Redis客户端（可选）
aioredis==2.0.1  # 异步Redis客户端（可选）
